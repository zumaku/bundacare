import 'package:flutter/material.dart';

class DetectPage extends StatelessWidget {
  const DetectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Detect')),
      body: const Center(child: Text('Detect screen placeholder: show result and Save button')),
    );
  }
}
